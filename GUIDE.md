# 全流程说明（纯讲解文件）

> 本文件不含任何可运行的代码，纯粹用来讲清楚：
> **一个"智能合约 + 前端"项目从零到上线的完整流程是什么、每一步在做什么、为什么这么做。**
>
> 建议先通读一遍，做项目时对照 [CHECKLIST.md](CHECKLIST.md) 打勾执行。

---

## 一、总览：一条不变的流水线

```
┌────────┐   ┌────────┐   ┌────────┐   ┌────────┐   ┌────────┐   ┌────────┐
│ 写合约 │ → │  编译  │ → │ 部署上链│ → │ 开源验证│ → │ 写前端 │ → │ 托管发布│
└────────┘   └────────┘   └────────┘   └────────┘   └────────┘   └────────┘
```

除了"写合约"是每个项目的创作部分，其余五步的**动作每次都一样**——这正是本模板存在的意义。

---

## 二、每一步在做什么

### 第 1 步：写合约（contracts/ 文件夹）

**做什么**：用 Solidity 写一份合约，定义你的应用的数据和规则。

**为什么**：合约就是你的"后端服务器"，只不过它跑在区块链上——不归任何人管、永久运行、谁都能查。

**每份合约必有的四要素**：
1. SPDX 许可证标识（开源惯例）
2. pragma 编译器版本
3. 状态变量——你的数据存在哪
4. 函数 + 事件——谁能读、谁能写、改动留痕

**核心思维**：写合约前先问自己三个问题：
- 数据存什么？（比如：一句话、一个数字、一组名单）
- 谁能读？（view 函数，免费，人人可调）
- 谁能写？（会改变链上状态，调用者要付 gas）

### 第 2 步：编译（npx hardhat compile）

**做什么**：把 Solidity 源码翻译成机器能执行的字节码。

**为什么**：区块链只认识字节码，不认识 Solidity。

**副产品**：编译会生成 `artifacts/` 文件夹，里面存着 ABI——前端和链上合约对话的"说明书"。这一步很重要，后面第 5 步要用。

### 第 3 步：部署上链（scripts/deploy.ts）

**做什么**：把编译好的合约发布到区块链上，得到一个永久的合约地址。

**为什么**：合约只有上链后才真实存在，别人才调得到。

**关键动作**：
- 部署账户付 gas（所以钱包里要有测试币）
- 部署时给构造函数传参（合约的初始状态）
- **把打印出的合约地址回填到 .env 的 CONTRACT_ADDRESS**——忘了这步，后面全卡住

**本地 vs 真链**：不带 `--network sepolia` 时跑在本地模拟链（免费、秒完成，适合调试）；带上才是真上链。

### 第 4 步：开源验证（npx hardhat verify）

**做什么**：把合约源码公开到 Etherscan 上，证明"这个地址的合约长这样"。

**为什么**：区块链上只存字节码，别人看不懂。验证后，任何人可以在 Etherscan 上直接读源码、直接调用读函数、查看所有历史交易。**公开透明是 dApp 的生命线**——不验证的合约没人敢信任。

**两个易错点**：
- 构造参数必须和部署时**完全一致**
- 国内网络要先设代理环境变量（见 CHECKLIST.md 第 5 步）

### 第 5 步：写前端（frontend/index.html）

**做什么**：做一个网页，让用户点按钮就能读链、写链，不用碰命令行。

**为什么**：合约是"后端"，但没人会用命令行操作你的产品。前端就是"界面"。

**前端永远只做两件事**：

| | 读（view 函数） | 写（改状态） |
|---|---|---|
| 需要钱包吗 | 不需要 | 需要（签名 + 付 gas） |
| 花钱吗 | 免费 | 付 gas |
| 用的工具 | JsonRpcProvider + 公共 RPC | 用户钱包 → signer |
| 流程 | 连节点 → 调函数 → 显示 | 发现钱包 → 检查网络 → 签名 → 发交易 → 等打包 → 刷新 |

**每次必改的三样东西**：
1. 合约地址
2. ABI（从 artifacts 里复制，只取用到的函数和事件）
3. 读写函数名

**⚠️ 新手头号坑：改了合约 → 重新编译 → 必须重新复制 ABI 到前端**。ABI 不同步，前端就会读到错数据甚至报错。

**前端的三条底线**：
- 绝不放私钥
- 绝不放带 key 的 API 地址（用公共 RPC）
- 只放公开信息：合约地址、ABI、RPC

### 第 6 步：托管发布（GitHub Pages）

**做什么**：把网页放到公网上，让全世界的人都能访问。

**为什么**：本地文件只有你自己看得到；你的目标是让"更多人"用。

**固定动作**：
1. git 三件套：`add` → `commit` → `push`
2. 每个新仓库开一次开关：Settings → Pages → Source 选 GitHub Actions
3. 等 Actions 里 deploy 工作流变绿勾
4. 网址就是 `https://<用户名>.github.io/<仓库名>/`

**以后每次更新**：改代码 → `git add .` → `git commit -m "说明"` → `git push`，约一分钟后线上自动更新。

---

## 三、贯穿全流程的核心概念

### 1. 钱包三件套（分清这三样，安全无虞）

| 东西 | 长什么样 | 用途 | 保密级别 |
|---|---|---|---|
| 地址 | 0x + 40 位（42 字符） | 公开身份，收款、显示 | 随便公开 |
| 私钥 | 0x + 64 位（66 字符） | 签名权，付 gas | 绝密，只在 .env |
| 助记词 | 12/24 个英文单词 | 恢复钱包的一切 | 绝密，抄在纸上 |

### 2. gas：每次写操作都要付"手续费"

- 读（view）：免费，不广播交易
- 写（改状态）：付 gas，矿工打包后生效
- 测试网上 gas 也是测试币——从水龙头免费领

### 3. 交易的一生

```
你点按钮 → 钱包签名 → 交易广播到全网 → 矿工打包进区块 → 链上状态改变
                                   ↑ 这个"等待"就是 tx.wait()
```

### 4. 事件日志：改动的永久"史书"

- 每次写操作发出的事件，永久留在链上日志里
- 所以旧数据"不会消失"——新的状态覆盖显示，但每次变化都有记录可查
- 这是区块链比普通数据库强的地方：无法篡改的完整历史

### 5. ABI：前端和合约之间的"翻译官"

- 合约编译后，机器只认识字节码
- ABI 告诉前端："这个合约有哪些函数、参数长什么样、返回什么"
- 合约改了，ABI 就变了，前端必须同步更新

---

## 四、三条铁律（违反任何一条都可能出事）

1. **私钥永不进 git、永不进前端。** 它只活在 `.env` 里（`.gitignore` 保护着它）。
2. **前端只用公共 RPC，绝不放带 key 的地址。** 放进前端的 key 等于送给全世界。
3. **每笔写操作都先想 gas。** 测试网上便宜，主网上每一笔都是真钱——先测试网跑通，再考虑主网。

---

## 五、典型时间线（第一次做，大概半天到一天）

| 阶段 | 耗时 | 备注 |
|---|---|---|
| 写合约 + 编译 | 1~2 小时 | 最花心思的部分 |
| 部署 + 验证 | 半小时 | 卡住多半是网络/参数问题 |
| 前端 | 1~2 小时 | 骨架复用，只改三样 |
| 发布 | 半小时 | 第一次建仓库略繁琐，以后 5 分钟 |

做完第一遍，第二遍开始每个环节都会快得多——这正是"套路"的力量。
